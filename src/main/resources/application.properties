# Puerto de la aplicación
quarkus.http.port=${QUARKUS_HTTP_PORT}

# --- Configuración administrativa para crear usuarios, roles, etc. ---
keycloak.auth-server-url=${KEYCLOAK_URL}
keycloak.realm=${KEYCLOAK_REALM}
keycloak.resource=${KEYCLOAK_CLIENT_ID}
keycloak.credentials.secret=${KEYCLOAK_CLIENT_SECRET}
keycloak.ssl-required=external
keycloak.public-client=true
keycloak.bearer-only=false
keycloak.admin.username=${KEYCLOAK_ADMIN_USER}
keycloak.admin.password=${KEYCLOAK_ADMIN_PASSWORD}

# --- Configuración OIDC para proteger endpoints y validar tokens ---
# Configuración de la aplicación
quarkus.application.name=api-user
quarkus.http.port=${QUARKUS_HTTP_PORT}

# Configuración de Keycloak OIDC
quarkus.oidc.auth-server-url=${KEYCLOAK_URL}realms/${KEYCLOAK_REALM}
quarkus.oidc.client-id=${KEYCLOAK_CLIENT_ID}
quarkus.oidc.credentials.secret=${KEYCLOAK_CLIENT_SECRET}

# Configuración para permitir acceso a endpoints específicos sin autenticación OIDC
quarkus.http.auth.permission.permit1.paths=/auth/*
quarkus.http.auth.permission.permit1.policy=permit
quarkus.http.auth.permission.permit2.paths=/users/create
quarkus.http.auth.permission.permit2.policy=permit
quarkus.http.auth.permission.permit3.paths=/q/swagger-ui/*
quarkus.http.auth.permission.permit3.policy=permit
quarkus.http.auth.permission.permit4.paths=/q/openapi
quarkus.http.auth.permission.permit4.policy=permit

# Autenticación requerida para el resto de endpoints
quarkus.http.auth.permission.deny1.paths=/*
quarkus.http.auth.permission.deny1.policy=authenticated

# Keycloak Admin Client
keycloak.url=${KEYCLOAK_URL}
keycloak.realm=${KEYCLOAK_REALM}
keycloak.admin.user=${KEYCLOAK_ADMIN_USER}
keycloak.admin.password=${KEYCLOAK_ADMIN_PASSWORD}
keycloak.client.id=${KEYCLOAK_CLIENT_ID}
keycloak.client.secret=${KEYCLOAK_CLIENT_SECRET}

# Token de autenticación para endpoints públicos
client.token=${AUTH_CLIENT_TOKEN}

# Configuración de logs
quarkus.log.console.level=${QUARKUS_LOG_CONSOLE_LEVEL}

# Configuración de PostgreSQL
quarkus.datasource.db-kind=postgresql
quarkus.datasource.jdbc.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
quarkus.datasource.username=${DB_USERNAME}
quarkus.datasource.password=${DB_PASSWORD}

# Hibernate ORM
quarkus.hibernate-orm.database.generation=${QUARKUS_HIBERNATE_ORM_DATABASE_GENERATION}
quarkus.hibernate-orm.log.sql=true
quarkus.log.category."org.hibernate.SQL".level=DEBUG
# quarkus.oidc.token.issuer=keycloak  # Comentado para evitar conflicto con el issuer del token
quarkus.oidc.application-type=service
quarkus.http.auth.proactive=true

# Configuración de autenticación HTTP - orden de las reglas importa
# Permitir acceso público a OpenAPI y Swagger UI
quarkus.http.auth.permission.public.paths=/q/*,/openapi,/openapi/*,/swagger-ui,/swagger-ui/*
quarkus.http.auth.permission.public.policy=permit

# Permitir acceso público al endpoint de login
quarkus.http.auth.permission.login.paths=/auth/login
quarkus.http.auth.permission.login.policy=permit

# Permitir acceso público al endpoint de creación de usuario
quarkus.http.auth.permission.createuser.paths=/users
quarkus.http.auth.permission.createuser.policy=permit

# Permiso por defecto para proteger el resto de los endpoints
quarkus.http.auth.permission.default.paths=/*
quarkus.http.auth.permission.default.policy=authenticated

# SmallRye OpenAPI
# Habilitar OpenAPI y Swagger UI explícitamente
quarkus.smallrye-openapi.path=/openapi
quarkus.swagger-ui.always-include=true
quarkus.swagger-ui.path=/swagger-ui
# OpenAPI está habilitado por defecto en Quarkus
quarkus.log.console.level=INFO

# Token esperado para el login
auth.client-token=${AUTH_CLIENT_TOKEN}
